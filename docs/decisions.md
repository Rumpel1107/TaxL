# Decisions

> **What goes in this document:** why something already settled was settled, with the alternative
> that was rejected. Append-only.
>
> Not here: what is still pending (`docs/roadmap.md`), what happened (`docs/log.md`), tax rules
> and their sources (`docs/domain/rules.md`).

**A row is never edited or deleted once accepted.** Changing your mind adds a new row and flips
the old row's status. Ids are stable and never reused.

| Status | Meaning |
|---|---|
| `accepted` | In force |
| `superseded by Dnn` | Replaced by a later decision |
| `deprecated` | Stopped applying, with nothing replacing it |

Rows carried over from `PLAN.md` §13 and from the discovery sessions recorded in
`docs/context/CONTEXT.md` §5. Where the original record did not preserve the rejected
alternative, the cell says **not recorded** rather than a reconstruction.

## Product and scope

| # | Date | Decision | Rejected alternative | Why | Status |
|---|---|---|---|---|---|
| D01 | — | North Star = "Understand and verify before filing" | not recorded | The DIAN already fills the form; the gap is understanding it | `accepted` |
| D02 | — | MVP profile = employee | Covering several taxpayer profiles at once | Cut by profile to keep the riskiest logic small | `accepted` |
| D03 | — | Validation suite = 3 real reference returns + webinar examples | Synthetic test cases | Free ground truth that is already known to be correct | `accepted` |
| D04 | 2026-07-06 | Monetization funnel: free ceiling estimate → X (full liquidation) → Y (next-year recommendations) | Single paid product | The free estimate is deliberately the ceiling, so X sells "see how much you can lower it" | `accepted` |
| D05 | 2026-07-06 | Product entry point = exógena file upload | Manual data entry as the entry point | Nobody enters their data with discipline; manual entry stays as fallback | `accepted` |
| D06 | 2026-07-06 | Exógena parsing moved P3 → P2 | Leaving it in P3 | It sits on the free tier's critical path; accepts advancing the R11–R14 technical risk | `accepted` |
| D07 | 2026-07-06 | Form 210 draft output stays post-P2, announced in X as "coming soon"; tier Y gated on legal-boundary research | Shipping the draft output inside X | High value but high consequence on error | `superseded by D42` |
| D08 | 2026-07-06 | No committed deadline; Tabris and P0 advance in parallel | Fixing a date for the 2026 season | Entering mid-season 2026 is acceptable for early validation | `accepted` |
| D09 | 2026-08-10 | C2 — MVP automates one reference case exactly, plus out-of-scope detection | Speculative coverage of other profiles | Coverage expands case by case as real scenarios appear | `accepted` |
| D10 | 2026-08-10 | C3 — Parameters editable by non-technical users (accountants) as a first-class feature | Parameters as config files | An accountant must be able to update a legal value without a developer | `accepted` |
| D34 | 2026-08-27 | Product UI defaults to English, with Spanish selectable per user | A Spanish-only UI, matching the Colombian tax domain | The products are also portfolio pieces, and the segment of most interest is foreign clients | `accepted` |
| D37 | 2026-08-31 | Benefits the taxpayer did not apply are pointed out in the manual PoC, framed for validation with their accountant; the product blocks them until R-9 closes | Offering them in the product from its first version, or withholding them in the PoC too | The analysis surfaces them either way; R-9 gates offering it as a feature, not knowing the figure | `accepted` |
| D46 | 2026-09-03 | The choices the law leaves to the taxpayer are presented on the result screen, each with the figure it produces, and the user picks; the advance payment additionally accepts a figure typed by the user or their accountant | Asking for each choice up front, before the user can see what it changes | Both choices belong to the taxpayer and both move the final number, so deciding them blind is not a decision. The typed advance also lets the engine reproduce a reference return whose advance neither lawful base explains, which is R9's open flag | `accepted` |
| D41 | 2026-09-03 | The out-of-scope notice records which fields the engine did not cover, and that report is what prioritizes new coverage | Failing loudly and leaving no trace of the case | Principle 9 grows coverage case by case as real scenarios appear, and no document said how those cases were learned. The report stores the field, never the value | `accepted` |
| D56 | 2026-09-10 | AC7, two values that contradict each other, leaves the manual-liquidation slice for the exógena epic (US-P2-004) | Keeping the criterion with an empty list of comparable pairs, to be filled case by case | The scenario is born of crossing what third parties reported against the user's own supporting documents, which is what `PLAN.md` §2 names as a differentiator of the exógena. Here the user transcribes their own documents by hand: there is one source per fact, so there is nothing to reconcile. Constitution principle 5 stands, and is met when there are two sources | `accepted` |
| D42 | 2026-09-03 | The line-by-line detail carries each value's Form 210 box number from the first slice | Showing the detail without box numbers, keeping the 210 output deferred (D07) | The user copies the figures with the form open anyway; hiding the box makes the result harder to verify, not safer. The disclaimer carries the consequence the deferral was protecting against | `accepted` |

## How the product treats data

| # | Date | Decision | Rejected alternative | Why | Status |
|---|---|---|---|---|---|
| D11 | 2026-08-10 | A1 — Exógena is the starting point only; official certificates win on conflict | Trusting exógena as authoritative | Two entities can report the same money; a trust company's disbursement reads as income | `accepted` |
| D12 | 2026-08-10 | A2 — Detect income *missing* from exógena via certificates | Treating exógena as complete | Found live: income from a foreign-payments provider absent from the file | `accepted` |
| D13 | 2026-08-10 | A3 — Field→calculation mapping is proprietary knowledge, not a label read-off | Mapping by the file's own labels | Labels are ambiguous: "susceptible de beneficio" is the FE field, "tras ajustes por notas" feeds the purchases threshold | `accepted` |
| D14 | 2026-08-10 | A4/A5 — On any discrepancy or unrecognized item, show the diff and let the user decide | Silently picking a side | The product sells trust; a silent choice is the one thing that destroys it | `accepted` |
| D15 | 2026-08-10 | B1 — Flow starts with exógena upload, which then produces the suggested-documents checklist | Asking for certificates up front | The file is what the user can get alone, in one place | `accepted` |
| D16 | 2026-08-10 | B4 — Show everything, including items with zero tax impact | Showing only what changes the result | Completeness builds trust; messaging distinguishes an error with impact from a form error | `accepted` |
| D17 | 2026-08-10 | B5 — Apply the law as written as the baseline; a professional or the user may override with professional judgment | Encoding a professional's judgment as the default | The tool prepares for the accountant, it does not replace them | `accepted` |
| D55 | 2026-09-10 | What the user types is held in the tab's own memory: it survives a reload, and closing the tab loses it. The server holds nothing between steps and sees the case only when a liquidation or the obligation gate is asked for | The browser's persistent memory, cleared when the case is submitted, so the user could come back the next day | Clearing on submit protects whoever finishes, who was never the exposed case; whoever abandons halfway never reaches the submit, and their figures stay on that disk — on a shared computer, for the next person. The tab's memory clears itself on the abandoned case too. Coming back the next day is a capability no user has asked for, and it needs an expiry, a delete button and a notice to be safe | `accepted` |
| D57 | 2026-09-10 | Law 1581 applies from this slice, not from P2: the point where the email is asked for carries what is stored, what for, the retention rule and how to ask for deletion, with a channel that answers | Keeping the deferral to P2 recorded in `PLAN.md` §5 and in risk R-6 | A consented email is a third party's personal data, so R-6's trigger — the first user other than the owner — is met by the first user of the first slice, not in P2. The cost is a text and a contact address; the owner already reads that table to prioritize coverage, so deleting a row on request is not infrastructure. The exact obligations are still to be confirmed by the legal-boundary research | `accepted` |
| D40 | 2026-09-03 | The first slice runs as guest: the user's fiscal figures are never persisted, and the only things stored are the email the user consents to give and the out-of-scope field report | Accounts and persistence of the user's data from the first slice | Nothing is persisted until its custody can be guaranteed, and accounts make no sense before the tool can charge — that is when Law 1581 (risk R-6) is revisited | `accepted` |

## Product positioning

| # | Date | Decision | Rejected alternative | Why | Status |
|---|---|---|---|---|---|
| D18 | 2026-08-10 | B2 — The in-cap / out-cap (40%) bucket architecture is made visible as the core pedagogical differentiator | Hiding the mechanics behind a final figure | It is the part no competitor explains | `accepted` |
| D19 | 2026-08-10 | B3 — "Exceso perdido" (benefits lost to the cap) is the candidate headline insight | Leading with the tax figure | Across three real years the lost benefit ran into tens of millions of pesos | `accepted` |
| D20 | 2026-08-10 | B6 — "Validate a past return" is a hypothesis for the professional segment | Treating contadores as out of scope | To be tested in the M5 probe, not assumed | `accepted` |

## Method and repository

| # | Date | Decision | Rejected alternative | Why | Status |
|---|---|---|---|---|---|
| D21 | — | Backlog and plan live as version-controlled markdown in the repo | A tracker as the source of truth | Single source of truth, readable by agents | `accepted` |
| D22 | — | Board tool = GitHub (Issues + Projects) | not recorded | Never implemented, and `/method` plus `docs/roadmap.md` now cover what it was for. Whether a board is still wanted is an open roadmap item ("Decide whether a board is needed on top of `/method`") | `deprecated` |
| D23 | 2026-07-06 | Owner tags `[R]` / `[A]` / `[R+A]` adopted | Treating every task as equally delegable | Marks who owns the outcome, not who types | `accepted` |
| D24 | 2026-07-06 | Parameter values written into acceptance criteria require a rule sheet before being trusted | Trusting webinar-sourced values because they were already written down | Written ≠ verified | `accepted` |
| D25 | 2026-08-10 | D2 — Ground truth is reconstructed from original source documents, never copied from prior results | Reusing the previous year's computed figures | This is how every TY2023 finding surfaced | `accepted` |
| D26 | 2026-08-10 | D3 — Golden tests run automatically on every change | Human review as the regression check | Two caps broke silently when the 2025 file was rebuilt by copy | `accepted` |
| D27 | 2026-08-24 | `/method` is the single process; the methodology sections in `PLAN.md` and `memory/constitution.md` are removed | Keeping the Kanban + DoR/DoD alongside `/method` | Three overlapping processes meant none was followed | `accepted` |
| D28 | 2026-08-24 | Documentation split into `PLAN.md` (framing), `docs/roadmap.md`, `docs/decisions.md`, `docs/log.md`, `docs/domain/` | Keeping one `PLAN.md` | Append-only content was burying the volatile content; `PLAN.md` §11 went stale unnoticed | `accepted` |
| D29 | 2026-08-24 | The agent applies changes to files; the owner reviews | "Propose, don't apply" | Applying edits by hand in this environment is friction with no benefit | `accepted` |
| D30 | 2026-08-24 | Decisions carry an id and a status, and are never edited once accepted | Editing or deleting a row when it stops applying | Editing destroys the record that the earlier reasoning existed; D22 needed this the day it was written | `accepted` |
| D31 | 2026-08-27 | Nothing committed may identify the owner or a tester. Every figure, name and example in the repository is illustrative, even when modelled on a real case | Committing the real case with the identifying fields removed | The repository is public, and a real return re-identifies whoever filed it | `accepted` |
| D32 | 2026-08-27 | Personal tax administration is not roadmap work and is tracked outside the repository | Keeping the owner's own filing tasks in `docs/roadmap.md` | They are not product work, and they published a filing calendar tied to a real taxpayer | `accepted` |
| D33 | 2026-08-27 | The real source documents stay in the repository folder, gitignored, and are deleted once the app runs without them | Moving them to a sibling folder outside the repository | They are the reference material the app is built against; git already ignores them, and what they must never turn into is governed by D31 | `accepted` |
| D35 | 2026-08-27 | Project documents are English; Spanish survives only in acronyms, normative citations and `exógena`. `ficha` became `rule sheet` | Keeping Colombian tax terms in Spanish throughout the documentation | A citation loses its link to the source when translated; a domain term does not. The mix had already spread across twelve files | `accepted` |
| D36 | 2026-08-27 | A rule sheet carries values, source, formula, conditions and test case; explanatory prose is dropped | Keeping "the rule in the owner's own words" as the core of the sheet | The sheet is the engine's specification, not a reader's explanation; a sentence that does not change what the engine does is noise | `accepted` |
| D43 | 2026-09-02 | The stack is chosen and the first vertical slice is built without waiting for P0 to close | Keeping every line of code behind P0 | What remains of P0 is validation work that depends on other people answering and blocks no code, while the domain risk of building on a rule still at 🟡 is what the golden tests are for — and it does not shrink by waiting | `accepted` |
| D38 | 2026-09-02 | A registry row may close inside another rule's sheet: it keeps its row as the index and names where it closes, with no content duplicated. R18 closes inside R3 | Writing a separate sheet for R18, or deleting its row because R3 already covers it | The registry indexes every rule the engine implements, so deleting the row loses the entry point; a second sheet would repeat R3's formula and test case, and the two copies are what drift apart | `accepted` |
| D45 | 2026-09-03 | A running UI is inspected and evidenced with Playwright: it drives a real browser while the work is being built, and it holds the acceptance criteria as end-to-end tests | Chrome DevTools MCP alongside Playwright, or the owner checking the screen by hand as the only mechanism | One tool covers both uses, and the tests it leaves are the acceptance criteria themselves, so the evidence is not extra work. The owner's own look stays as the closing check of each slice, never as the only one | `superseded by D54` |
| D44 | 2026-09-03 | Stack: the engine and its API in Java 21 with Spring Boot, the interface in React with TypeScript, user-facing text in per-language key files, and a minimal database holding only consented emails and out-of-scope reports | Running the engine in the browser alongside the interface, with no server at all | The paid tier needs a server anyway for accounts, payment and exógena, so a browser-only engine would be rewritten within months — and the engine is the one piece that must not be rewritten. Strict types and exact decimal arithmetic are what make it verifiable by reading, which is what matters when the owner is not the one typing | `superseded by D47` |
| D47 | 2026-09-09 | The stack stands as D44 described it — Java 21 with Spring Boot for the engine and its API, React with TypeScript for the interface, user-facing text in per-language key files — and the database also holds the legal parameters of each tax year, besides the consented emails and the out-of-scope reports | Keeping the legal parameters in versioned files inside the repository | The professional who answers for the accuracy of those values has to be able to change them without touching the repository, and that means a table an admin panel writes to. Restated in full rather than amended, because D44's database clause stopped being true and a row is never edited | `accepted` |
| D48 | 2026-09-09 | The legal parameters carry no change history while the repository's initial load is the only thing that writes them, and the tax year is part of the key from the first day so that adding one later is additive; the history ships with the admin panel that makes it necessary | Writing a versioned history of every parameter change from the first slice | While nothing can change a value without passing through the repository, git already is that history — author, date and diff. Building the table now would be building for a case that does not exist yet | `accepted` |
| D49 | 2026-09-09 | CI runs the suite on every push against an invented reference case built for it; the three real returns stay in the local fixture and the owner runs them himself before each delivery | Leaving the golden tests outside the automatic gate altogether, or putting real expected values where CI could read them | Principle 3 keeps the real expected values out of the repository, so a suite running in CI without them is green without having compared anything. The invented case catches ordinary breakage before it reaches the owner; it does not carry the authority of the three real returns, which stay the ground truth (D03) | `accepted` |
| D50 | 2026-09-09 | A slice zero comes before the engine: the end-to-end skeleton, deployed on the owner's server and served over HTTPS | Building the whole first slice locally and deploying once it is finished | Deploying an empty skeleton is cheap, and the first deployment is where the problems nobody foresaw appear — meeting them with the engine already on top is what makes them expensive. It also gives the "productive" environment the owner already works with on Tabris | `accepted` |
| D51 | 2026-09-10 | The engine is plain Java inside the same project, depending on nothing around it — not Spring, not the database, not the text files: it receives the case and the year's parameters already read, and returns the liquidation | A separate project from the first day, consumed by the server as an external piece | The independence is what the constitution protects, and it comes from the rule, not from the separation: hundreds of cases run in seconds without a server, and the fontanería can change without touching the calculation. A second project charges versioning and synchronizing from day one for a benefit — reusing the engine elsewhere — that does not exist yet, and going from one to the other later is moving files, not rewriting logic | `accepted` |
| D52 | 2026-09-10 | Every line the engine returns carries its identity attached: the Form 210 box, the article behind it and the inputs it came from | Returning the figures, and pasting box and article afterwards from a table of their own | Two truths about the same line drift without anyone noticing — the figure stays right and the box goes wrong, which is worse than a visible error. With the trace inside the result, the golden tests can assert it, and boxes and articles travel with the year's parameters, where they already belong | `accepted` |
| D53 | 2026-09-10 | The engine and the API send identifiers; every phrase of both languages lives in the interface's text files, and the closed list of uncovered fields travels as identifiers too | Sending the text already translated from the server | The engine knowing about languages contradicts D51, and the server would carry a second copy of what the interface already has. A third language becomes a text file. What gets stored is the identifier, which is what the owner reads to prioritize, not a Spanish phrase | `accepted` |
| D54 | 2026-09-10 | Verification is split by nature: Playwright drives a real browser for what the user sees and does, the engine is tested directly for the calculation, the tax years and the sum of certificates, and the server criteria are verified against the API and against the deployment. Playwright remains the tool for the UI and the owner's own look remains the closing check, as D45 established | The 22 criteria through the browser, which is what D45 said literally | Verifying the peso-exact liquidation means hundreds of cases — every cap, every bracket boundary, every combination of taxpayer choices. Through the browser each one fills the form step by step and takes seconds; nobody waits half an hour, so the suite the constitution calls inviolable would be the first to be switched off. Against the engine those same cases run in seconds | `accepted` |
| D39 | 2026-09-03 | Specs live in `docs/specs/<name>/`, named without a number | Numbered `NNN-name` directories, and `docs/reference/` as their home | Execution order already lives in the roadmap, and a second copy of it in folder names is what drifts — the topic-based numbering had already made the first spec to be written `003`. `docs/reference/` is defined as closed input material, which a live spec breaks | `accepted` |
